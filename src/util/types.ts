export interface NftType {
  minterAddres: string;
  ownerOf: string;
  symbol: string;
  tokenAddress: string;
  tokenId: string;
  tokenUri: string;
  image: string;
  name: string;
  isFavorited: boolean;
}
